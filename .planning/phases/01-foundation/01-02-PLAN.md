---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/dashboard/page.tsx
  - src/app/master/page.tsx
  - src/app/roles/[roleSlug]/page.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/layout/topbar.tsx
  - src/components/layout/app-shell.tsx
  - src/lib/constants.ts
autonomous: true
requirements:
  - FOUND-05
  - ROLE-01
must_haves:
  truths:
    - "Visiting localhost:3000 redirects to /dashboard"
    - "The sidebar shows Dashboard, Master View at the top, a ROLES section with 4 default roles (Video Editor, Writer/Scriptwriter, Designer, AI/Tech), a + New Role link, and Settings pinned at the bottom"
    - "Clicking a role in the sidebar navigates to /roles/[roleSlug] which shows the role name, a tab strip of all roles, and a No candidates yet empty state"
    - "The topbar shows HireFlow logo, a search input (disabled/placeholder), and a mock user avatar"
    - "On mobile viewport, the sidebar collapses to an overlay accessible via hamburger menu"
    - "On tablet viewport, the sidebar collapses to icon-only mode"
    - "The dashboard page shows stats bar (all zeros), 4 default role cards + Create New Role card, all with light theme styling"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Sidebar with grouped navigation and role list from database"
      contains: "SidebarGroup"
    - path: "src/components/layout/topbar.tsx"
      provides: "Top bar with logo, search, and mock avatar"
      contains: "Topbar"
    - path: "src/app/layout.tsx"
      provides: "Root layout wrapping SidebarProvider"
      contains: "SidebarProvider"
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard with stats and role cards"
      contains: "Dashboard"
    - path: "src/app/roles/[roleSlug]/page.tsx"
      provides: "Role detail page with tab strip and empty state"
      contains: "roleSlug"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/db/index.ts"
      via: "fetches active roles for sidebar"
      pattern: "db\\.select.*roles"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/app-sidebar.tsx"
      via: "roles prop passed server-side"
      pattern: "<AppSidebar roles="
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/app/roles/[roleSlug]/page.tsx"
      via: "Link href to role slug"
      pattern: "roles/.*slug"
    - from: "src/app/dashboard/page.tsx"
      to: "src/db/index.ts"
      via: "fetches roles for role cards"
      pattern: "db\\.select.*roles"
---

<objective>
Build the complete app shell — sidebar navigation, topbar, responsive layout — and all page skeletons (dashboard, master view, role pages) with proper routing and empty states.

Purpose: This gives the app its navigable structure. After this plan, every route works and renders correctly with light theme styling, even though no candidate data exists yet.
Output: Full app shell with sidebar, topbar, dashboard (empty state), role pages (empty state), master view (empty state), and root redirect.
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build app shell — root layout, sidebar, and topbar</name>
  <files>
    src/app/layout.tsx
    src/components/layout/app-sidebar.tsx
    src/components/layout/topbar.tsx
    src/components/layout/app-shell.tsx
    src/lib/constants.ts
  </files>
  <action>
1. Create `src/lib/constants.ts`:
   - Define `MOCK_USER = { name: 'Vishal', avatar: null }` — used wherever user context is needed until auth is added.
   - Define `LUCIDE_ROLE_ICONS` — array of ~20 creative-role-relevant Lucide icon names: Film, PenLine, Palette, Cpu, Camera, Music, Mic, Video, Brush, Scissors, Figma, Code, BookOpen, Image, Sparkles, Megaphone, PenTool, Monitor, Clapperboard, Pencil. This is the icon picker set for role creation.
   - Define `STATUS_LABELS` — record mapping candidateStatusEnum values to display labels (e.g., 'left_to_review' -> 'Left to Review').
   - Define `STATUS_COLORS` — record mapping each status to a softer/pastel Tailwind color class (bg + text) that works on light backgrounds. Green family for positive (shortlisted, hired), red family for negative (rejected, not_good, assignment_failed), blue for review states, amber for maybe/pending, purple for assignment states.
   - Define `TIER_LABELS` and `TIER_COLORS` similarly.

2. Create `src/components/layout/topbar.tsx`:
   - A horizontal bar at the top of the content area (not inside sidebar).
   - Left: "HireFlow" text logo in semibold, blue (#3B82F6) accent.
   - Center: Search input using shadcn Input — `placeholder="Search candidates..."`, `disabled={true}` for Phase 1. Add a Search icon (lucide) inside the input.
   - Right: Mock user avatar — a circle with initials (first letter of MOCK_USER.name) and a DropdownMenu with the mock user name displayed, and a disabled "Sign out" item.
   - Use `SidebarTrigger` from shadcn sidebar to render the hamburger/collapse button on the left edge (this controls mobile overlay and tablet collapse).

3. Create `src/components/layout/app-sidebar.tsx`:
   - Accept `roles: Role[]` prop (fetched server-side in layout).
   - Use shadcn `Sidebar` with `collapsible="icon"` — this gives desktop full sidebar, icon-only on collapse, and mobile overlay.
   - **Structure (top to bottom):**
     - `SidebarHeader`: HireFlow logo (small, for collapsed state shows icon only).
     - `SidebarContent`:
       - First `SidebarGroup`: Dashboard link (LayoutDashboard icon) + Master View link (Users icon). No group label — these are top-level.
       - Second `SidebarGroup`: `SidebarGroupLabel` = "ROLES". `SidebarMenu` with one `SidebarMenuItem` per role — each renders the role's Lucide icon dynamically and the role name, linking to `/roles/${role.slug}`. After all roles, a "+ New Role" menu item linking to `/settings` (PlusCircle icon).
     - `SidebarFooter`: Settings link (Settings icon) linking to `/settings`.
   - For dynamic Lucide icons: create a small helper component `DynamicIcon` that takes an icon name string and renders the corresponding Lucide icon. Use a record/map of the ~20 icons from LUCIDE_ROLE_ICONS to their components. Fallback to Briefcase if icon name not found.
   - **Light sidebar styling**: The sidebar background should be white or very light grey (#F8FAFC) with a subtle right border. NOT dark navy. Use shadcn sidebar's CSS variables or override with Tailwind.
   - Highlight the active route (current pathname) — use `usePathname` in a client wrapper if needed, or pass active path as prop.

4. Create `src/app/layout.tsx` (root layout):
   - Import Inter font from `next/font/google` (or Geist if available in the scaffold — use Claude's discretion for best productivity-app feel).
   - Server component that fetches active roles: `db.select().from(roles).where(eq(roles.isActive, true)).orderBy(roles.sortOrder)`.
   - Wrap body content in `SidebarProvider`.
   - Render `AppSidebar` with fetched roles, then a flex column div containing `Topbar` and `<main className="flex-1 p-6 bg-gray-50/50">{children}</main>`.
   - Light theme: `<html lang="en" className="light">`, body background white/light.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <manual>Run `npm run dev`, visit localhost:3000 — sidebar should render with 4 default roles, topbar with search and avatar</manual>
  </verify>
  <done>Root layout renders sidebar (with 4 default roles from DB, grouped navigation, light theme) and topbar (logo, disabled search, mock avatar). Sidebar collapses to icons and has mobile overlay via SidebarTrigger.</done>
</task>

<task type="auto">
  <name>Task 2: Create all page skeletons — dashboard, role pages, master view, root redirect</name>
  <files>
    src/app/page.tsx
    src/app/dashboard/page.tsx
    src/app/master/page.tsx
    src/app/roles/[roleSlug]/page.tsx
  </files>
  <action>
1. Create `src/app/page.tsx`:
   - Server component that redirects to `/dashboard` using `redirect` from `next/navigation`.
   - No UI — just the redirect.

2. Create `src/app/dashboard/page.tsx`:
   - Server component. Fetch all active roles from DB with candidate count (will be 0 in Phase 1).
   - **Stats bar** at top: Row of stat cards showing "Total Candidates", "Left to Review", "Under Review", "Shortlisted", "Hired", "Rejected" — all showing 0. Use shadcn Card components. Each stat has an icon, label, and count. Muted styling since all are zero.
   - **Role cards grid** below stats: 2-column grid on desktop (use `grid-cols-1 md:grid-cols-2`), 1 column on mobile. Each role card shows:
     - Role icon (dynamic Lucide icon) + role name
     - "0 candidates" text
     - A "View All" link to `/roles/${role.slug}`
     - NO Import or Add Candidate buttons (deferred per CONTEXT.md)
   - **Create New Role card** at the end of the grid: dashed border card with PlusCircle icon and "Create New Role" text, linking to `/settings`.
   - Light theme: white cards with subtle border (`border-gray-200`), light grey page background.

3. Create `src/app/roles/[roleSlug]/page.tsx`:
   - Server component. Receives `params.roleSlug`.
   - Fetch the role by slug. If not found, show 404 or redirect to dashboard.
   - Fetch all active roles for the **tab strip** at top — horizontal row of role name pills/tabs. Current role is highlighted (blue). Each tab links to its role page.
   - **Role header**: Role icon + role name + "(0)" candidate count.
   - **Empty state**: Centered message "No candidates yet" with a brief description like "Candidates will appear here once imported or added manually." Use a muted icon (Users or Inbox) above the text.
   - Structure is ready for Phase 2 to replace the empty state with the candidate table.

4. Create `src/app/master/page.tsx`:
   - Server component. Master View across all roles.
   - Page header: "Master View" + "All Candidates" subheading.
   - Empty state: "No candidates yet" — same pattern as role pages but says "All candidates across every role will appear here."
   - This page will be populated in Phase 2.

5. Ensure all navigation links in sidebar work — Dashboard, Master View, each role, Settings. Verify no broken routes.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <manual>Visit each route: /, /dashboard, /master, /roles/video-editor, /roles/designer, /settings — all should render without errors</manual>
  </verify>
  <done>All routes work: root redirects to /dashboard, dashboard shows stats (zero) + role cards + create role card, each /roles/[slug] shows tab strip + empty state, /master shows empty master view. Navigation between all pages works via sidebar links.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. Visiting `/` redirects to `/dashboard`
3. Dashboard shows 6 stat cards (all zero), 4 role cards, and Create New Role card
4. Sidebar shows Dashboard, Master View, ROLES section with 4 roles, + New Role, and Settings
5. Each `/roles/[slug]` page shows tab strip with all 4 roles and empty state
6. `/master` shows empty master view
7. On mobile viewport (< 768px), sidebar collapses to overlay
8. On tablet viewport, sidebar shows icon-only mode
9. Light theme throughout — white/light grey backgrounds, blue accents
</verification>

<success_criteria>
- All routes render without errors
- Sidebar navigation works for all links
- Dashboard empty state is informative (not blank)
- Role pages show tab strip for role switching
- Responsive behavior: mobile overlay, tablet icon-only, desktop full sidebar
- Consistent light theme with blue (#3B82F6) accent
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
