---
phase: 02-candidate-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/candidates/candidate-table.tsx
  - src/components/candidates/candidate-row.tsx
  - src/components/candidates/candidate-add-row.tsx
  - src/components/candidates/status-badge.tsx
  - src/components/candidates/tier-badge.tsx
  - src/components/candidates/edit-field.tsx
autonomous: true
requirements:
  - CAND-02
  - ROLE-03
  - ROLE-04

must_haves:
  truths:
    - "Candidate table renders rows with 8 columns: Name, Email, Portfolio, Phone, Instagram, Status badge, Tier badge, Date Added"
    - "Status badge is clickable and opens a dropdown with all 12 pipeline statuses"
    - "Tier badge cycles on click through Untiered, Junior, Senior, Both"
    - "Inline add-candidate row expands in the table when Add button is clicked"
    - "Edit-field primitive switches between text display and input on click"
  artifacts:
    - path: "src/components/candidates/candidate-table.tsx"
      provides: "Table shell with header, rows, and add-candidate button"
      min_lines: 50
    - path: "src/components/candidates/candidate-row.tsx"
      provides: "Single candidate row with 8 columns"
      min_lines: 30
    - path: "src/components/candidates/status-badge.tsx"
      provides: "Clickable status badge with dropdown"
      min_lines: 25
    - path: "src/components/candidates/tier-badge.tsx"
      provides: "Clickable tier badge with cycle-on-click"
      min_lines: 20
    - path: "src/components/candidates/candidate-add-row.tsx"
      provides: "Inline form row for adding candidates"
      min_lines: 30
    - path: "src/components/candidates/edit-field.tsx"
      provides: "Click-to-edit field primitive"
      min_lines: 25
  key_links:
    - from: "src/components/candidates/status-badge.tsx"
      to: "src/lib/actions/candidates.ts"
      via: "imports changeStatus server action"
      pattern: "import.*changeStatus"
    - from: "src/components/candidates/tier-badge.tsx"
      to: "src/lib/actions/candidates.ts"
      via: "imports changeTier server action"
      pattern: "import.*changeTier"
    - from: "src/components/candidates/candidate-row.tsx"
      to: "status-badge.tsx and tier-badge.tsx"
      via: "renders StatusBadge and TierBadge components"
      pattern: "<StatusBadge|<TierBadge"
---

<objective>
Build all candidate UI components: the table shell, individual row, clickable status/tier badges, inline add form, and the click-to-edit field primitive.

Purpose: These are the core interactive components that make up the candidate list view. Building them as isolated components (with proper props interfaces) enables parallel integration in later plans.
Output: 6 component files in src/components/candidates/
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-candidate-core/02-RESEARCH.md
@src/types/index.ts
@src/lib/constants.ts
@src/components/ui/dropdown-menu.tsx
@src/components/ui/badge.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create status badge, tier badge, and edit-field primitives</name>
  <files>src/components/candidates/status-badge.tsx, src/components/candidates/tier-badge.tsx, src/components/candidates/edit-field.tsx</files>
  <action>
All 3 files are 'use client' components.

**status-badge.tsx — StatusBadge component:**
- Props: candidateId (string), status (CandidateStatus)
- Uses DropdownMenu from @/components/ui/dropdown-menu (already installed, uses @base-ui/react)
- Renders a colored badge (using STATUS_COLORS, STATUS_LABELS from constants) as the trigger
- Dropdown content lists all 12 statuses from CANDIDATE_STATUSES array
- Each item shows a small color dot + label
- onSelect calls changeStatus server action (import from @/lib/actions/candidates)
- Use useTransition for pending state — show opacity-50 while pending
- CRITICAL: Add e.stopPropagation() on the trigger's onClick to prevent row click from firing (Research pitfall 2)

**tier-badge.tsx — TierBadge component:**
- Props: candidateId (string), tier (Tier)
- Renders a colored badge (TIER_COLORS, TIER_LABELS from constants)
- onClick cycles: untiered -> junior -> senior -> both -> untiered (use TIERS array with modulo)
- Calls changeTier server action with fromTier and nextTier
- Use useTransition for pending state
- CRITICAL: Add e.stopPropagation() on click to prevent row click bubbling

**edit-field.tsx — EditField component:**
- Props: value (string), onSave ((newValue: string) => Promise<void>), placeholder (string, optional)
- NOT editing state: render as a span with cursor-text, hover:bg-gray-50 styling
- Click toggles to editing state, auto-focuses the input
- Editing state: render an input with border, saves on blur or Enter key
- Escape key cancels edit (resets draft to original value)
- Only calls onSave if the value actually changed (draft !== value)
- Show subtle loading state during save (useTransition)
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit src/components/candidates/status-badge.tsx src/components/candidates/tier-badge.tsx src/components/candidates/edit-field.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Three primitive components compile, use correct imports from UI kit and constants, handle click propagation correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create candidate table, row, and inline add-row components</name>
  <files>src/components/candidates/candidate-table.tsx, src/components/candidates/candidate-row.tsx, src/components/candidates/candidate-add-row.tsx</files>
  <action>
All 3 files are 'use client' components.

**candidate-row.tsx — CandidateRow component:**
- Props: candidate (Candidate type from @/types), onSelect ((candidate: Candidate) => void)
- Renders a table row (tr) with 8 cells matching CONTEXT.md columns:
  1. Name — bold text, truncated if long
  2. Email — text-gray-500, truncated
  3. Portfolio Link — clickable anchor (opens in new tab), show domain only (strip protocol), blue text. If null, show dash.
  4. Phone/WhatsApp — text-gray-500. If null, show dash.
  5. Instagram — text-gray-500, prepend @ if not already prefixed. If null, show dash.
  6. Status — StatusBadge component (clickable dropdown)
  7. Tier — TierBadge component (clickable cycle)
  8. Date Added — format as relative time or short date (e.g., "Mar 13" or "2d ago")
- onClick on the row calls onSelect(candidate) — but NOT when clicking status/tier badges (those have stopPropagation)
- Hover state: bg-gray-50

**candidate-add-row.tsx — CandidateAddRow component:**
- Props: roleId (string), onCancel (() => void)
- Renders as a table row (tr) with form inputs matching the table columns
- Cells: Name input (required), Email input, Portfolio URL input, Phone input, Instagram input, empty status cell, empty tier cell, Cancel/Save buttons cell
- Uses a form that calls createCandidate server action via formData
- Include a hidden input for roleId
- Show validation errors inline (red text below input)
- On successful save, call onCancel to collapse the row
- Use useActionState or manual form handling pattern from Phase 1

**candidate-table.tsx — CandidateTable component:**
- Props: candidates (Candidate[]), total (number), roleId (string), currentPage (number), totalPages (number)
- 'use client' — manages local state for: selectedCandidate (for drawer), showAddRow (boolean)
- Renders:
  1. An "Add Candidate" button (top-right) that toggles showAddRow
  2. A table with thead (8 column headers matching the row)
  3. If showAddRow is true, render CandidateAddRow as first tbody row
  4. Map candidates to CandidateRow components
  5. "Showing X of Y candidates" text below table
  6. If candidates is empty and showAddRow is false, show empty state (reuse existing empty state pattern)
- When a CandidateRow calls onSelect, set selectedCandidate (drawer integration comes in Plan 03)
- When showAddRow is active and a row is clicked, auto-dismiss the add row (set showAddRow to false)
- Style the table to feel like Linear's issue list: clean borders, compact rows, good spacing
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit src/components/candidates/candidate-table.tsx src/components/candidates/candidate-row.tsx src/components/candidates/candidate-add-row.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Table shell renders header + rows + add-row form, all components compile, row triggers onSelect on click, add-row submits via createCandidate server action</done>
</task>

</tasks>

<verification>
- All 6 component files compile: `npx tsc --noEmit`
- StatusBadge uses DropdownMenu from UI kit (not hand-rolled)
- TierBadge cycles through all 4 tiers
- EditField saves on blur/Enter, cancels on Escape
- CandidateRow renders all 8 columns
- CandidateAddRow submits via createCandidate server action
- Click propagation handled on status/tier badges
</verification>

<success_criteria>
- 6 component files in src/components/candidates/
- Table renders with correct 8-column layout
- Status badge dropdown shows all 12 statuses
- Tier badge cycles on click
- Inline add row collects all fields and submits
- Edit-field primitive works for click-to-edit pattern
- All components pass TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/02-candidate-core/02-02-SUMMARY.md`
</output>
