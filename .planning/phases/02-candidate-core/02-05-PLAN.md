---
phase: 02-candidate-core
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/master/page.tsx
  - src/components/candidates/candidate-table.tsx
  - src/components/layout/topbar.tsx
autonomous: false
requirements:
  - CAND-06
  - SRCH-02
  - SRCH-06
  - PIPE-04
  - PIPE-06

must_haves:
  truths:
    - "Master View at /master shows all candidates across all roles with a Role column"
    - "Global search in topbar searches across all roles simultaneously"
    - "Master view reuses the same CandidateTable component with an additional Role column"
  artifacts:
    - path: "src/app/master/page.tsx"
      provides: "Master view page showing all candidates with role join"
      min_lines: 30
    - path: "src/components/layout/topbar.tsx"
      provides: "Updated topbar with functional global search"
  key_links:
    - from: "src/app/master/page.tsx"
      to: "src/lib/queries/candidates.ts"
      via: "getCandidates called without roleId filter"
      pattern: "getCandidates"
    - from: "src/app/master/page.tsx"
      to: "src/components/candidates/candidate-table.tsx"
      via: "CandidateTable rendered with showRoleColumn prop"
      pattern: "<CandidateTable.*showRoleColumn"
---

<objective>
Build the Master View page (/master) showing all candidates across roles, add functional global search to the topbar, and handle deferred requirements (PIPE-04, PIPE-06, SRCH-06) with proper stubs.

Purpose: Master view gives a cross-role overview. Global search makes the topbar functional. Deferred requirements need explicit handling so they don't silently fall through.
Output: Updated master page, updated topbar, minor table update for role column.
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-candidate-core/02-RESEARCH.md
@.planning/phases/02-candidate-core/02-01-SUMMARY.md
@.planning/phases/02-candidate-core/02-02-SUMMARY.md
@src/app/master/page.tsx
@src/components/layout/topbar.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Master View page and add role column support to table</name>
  <files>src/app/master/page.tsx, src/components/candidates/candidate-table.tsx, src/components/candidates/candidate-row.tsx</files>
  <action>
**src/app/master/page.tsx — Master View page:**
- Server Component (no 'use client')
- Accept searchParams as Promise (same pattern as role page)
- Parse all filter params from URL (same as role page) PLUS a roleFilter param (?role=uuid)
- Call getCandidates WITHOUT roleId (pass undefined/omit) to get ALL candidates
- If roleFilter param exists, pass it to filter by role
- Fetch all active roles for role filter dropdown and for role name lookup
- Render layout:
  - Page header: "All Candidates" with total count
  - CandidateFilterBar (same component, add a role filter dropdown for master view)
  - CandidateTable with showRoleColumn={true} prop
  - CandidatePagination

For the role name display in the table, pass a roles lookup map (Record<string, string> of roleId -> roleName) to CandidateTable.

**Update CandidateTable (minor):**
- Add optional prop: showRoleColumn (boolean, default false)
- Add optional prop: rolesMap (Record<string, string>) for roleId -> roleName lookup
- When showRoleColumn is true, add a "Role" column header after "Name"
- Pass showRoleColumn and rolesMap down to CandidateRow

**Update CandidateRow (minor):**
- Add optional prop: showRoleColumn (boolean), rolesMap (Record<string, string>)
- When showRoleColumn is true, render a "Role" cell after Name showing the role name from rolesMap
- Style role name as a colored text or subtle badge

**SRCH-06 (Import Source filter) — DEFERRED per CONTEXT.md:**
Do NOT implement import source filter. This is explicitly deferred until Phase 3 when import pipeline exists. The filter bar already supports adding new filters — this can be added later.

**PIPE-06 (Bulk status change) — DEFERRED per CONTEXT.md:**
Do NOT implement bulk selection or bulk status changes. Explicitly deferred from Phase 2.

**PIPE-04 (Activity feed on dashboard with 30s refresh) — Phase 5 scope:**
Do NOT build the dashboard activity feed. Per Research analysis, PIPE-04 and PIPE-05 describe dashboard functionality which is Phase 5. The status history on individual candidate profiles (PIPE-03) is the Phase 2 deliverable. NOTE: These requirement IDs are listed in Phase 2's requirements but the actual implementation belongs in Phase 5 per the ROADMAP success criteria. Mark as "infrastructure ready" — the candidateEvents table and event logging from Plan 01 provides the data source Phase 5 will query.
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit src/app/master/page.tsx 2>&1 | head -20</automated>
    <manual>Verify master page renders all candidates, role column shows role names, deferred features are NOT implemented</manual>
  </verify>
  <done>Master view shows all candidates with Role column, reuses CandidateTable with showRoleColumn=true, deferred features (SRCH-06, PIPE-06, PIPE-04) are explicitly not implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add functional global search to topbar</name>
  <files>src/components/layout/topbar.tsx</files>
  <action>
Update the existing topbar component to make the search input functional:

1. Read the current topbar implementation to understand the existing structure
2. The search input in the topbar should be a global search (SRCH-02) — searches across ALL roles
3. Implementation approach: When user types in the global search and presses Enter (or after debounce), navigate to /master?q={searchTerm}
4. Use useRouter to navigate on Enter key
5. Use useDebounce (300ms) from @/hooks/use-debounce for the search value
6. When debounced value changes and is non-empty, navigate to /master?q={value}
7. When debounced value is cleared, if currently on /master, clear the q param
8. Add 'use client' directive if not already present (topbar needs interactivity for search)
9. If the topbar is currently a server component, convert the search input portion to a client component (extract SearchInput as a separate 'use client' component and import it into the topbar)

The global search should feel instant — type, wait 300ms, results appear in the master view.
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit src/components/layout/topbar.tsx 2>&1 | head -20</automated>
    <manual>Verify topbar search navigates to /master with q param</manual>
  </verify>
  <done>Global search in topbar navigates to /master?q=searchterm on debounced input, searches across all roles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete candidate workflow</name>
  <files>n/a</files>
  <action>
Human verification of the complete Phase 2 candidate management system. All automated tasks are complete — this checkpoint verifies the end-to-end workflow visually and functionally.

What was built across all 5 plans:
- Role-specific candidate tables with 8 columns
- Clickable status/tier badges
- Inline add-candidate form
- Profile drawer with editable fields and status history
- Filter bar with search/status/tier/date/sort/duplicates
- Pagination (50 per page)
- Master view with role column
- Global search in topbar
  </action>
  <verify>
1. Start dev server: `cd /Users/vishal.motion/HireFlow && npm run dev`
2. Navigate to any role page (e.g., /roles/video-editor)
3. Verify the tab strip shows all roles and the current role is highlighted
4. Click "Add Candidate" — verify inline form row appears in the table
5. Fill in name + email + portfolio URL, submit — verify candidate appears in table
6. Add 2-3 more candidates to have data to work with
7. Click the status badge on a candidate — verify dropdown shows all 12 statuses, select one, verify it changes
8. Click the tier badge — verify it cycles through Untiered/Junior/Senior/Both
9. Click a candidate row — verify profile drawer opens from the right
10. In the drawer, click a field to edit it — verify it becomes an input, saves on blur
11. Check status history in drawer — verify timeline shows the changes you made
12. Use the search input — type a name, verify filtering works after 300ms
13. Try the status filter (multi-select), tier pills, date filter — verify they combine
14. Check "Showing X of Y candidates" text updates with filters
15. Navigate to /master — verify all candidates from all roles appear with a Role column
16. Use global search in topbar — verify it navigates to /master with search results
  </verify>
  <done>All 16 verification steps pass. Complete candidate workflow is functional end-to-end.</done>
</task>

</tasks>

<verification>
- All files compile: `npx tsc --noEmit`
- Master view reuses CandidateTable component
- Global search navigates to /master with q param
- Deferred features (SRCH-06, PIPE-06) are NOT implemented
- PIPE-04/05 infrastructure is ready (events table + logging) but dashboard feed is Phase 5
</verification>

<success_criteria>
- Master view at /master shows all candidates with Role column and role filter
- Global search in topbar works across all roles
- Complete candidate workflow functional: add, view, edit, change status, change tier, search, filter
- All 30 Phase 2 requirement IDs accounted for (implemented or explicitly deferred with rationale)
</success_criteria>

<output>
After completion, create `.planning/phases/02-candidate-core/02-05-SUMMARY.md`
</output>
