---
phase: 04-ai-extraction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/import/StepUrlPaste.tsx
  - src/components/import/ImportWizard.tsx
  - src/components/import/ExtractionProgress.tsx
autonomous: true
requirements: [IMPT-08, IMPT-09]

must_haves:
  truths:
    - "User can paste one or multiple portfolio URLs and trigger extraction"
    - "Extraction runs in the background with a visible progress indicator"
    - "User can switch between file upload, paste text, and URL paste modes in the import wizard"
    - "Progress shows how many URLs are done out of total, with per-URL status"
  artifacts:
    - path: "src/components/import/StepUrlPaste.tsx"
      provides: "URL paste input with single/bulk mode, role selector, submit button"
      min_lines: 80
    - path: "src/components/import/ExtractionProgress.tsx"
      provides: "Progress bar + per-URL status list during extraction"
      min_lines: 60
    - path: "src/components/import/ImportWizard.tsx"
      provides: "Updated wizard with URL tab and extraction flow"
  key_links:
    - from: "src/components/import/StepUrlPaste.tsx"
      to: "src/lib/actions/extraction.ts"
      via: "calls startExtractions() or startSingleExtraction()"
      pattern: "startExtractions|startSingleExtraction"
    - from: "src/components/import/ExtractionProgress.tsx"
      to: "/api/extraction-status/[batchId]"
      via: "fetch polling every 2 seconds"
      pattern: "extraction-status"
---

<objective>
Add URL paste input and extraction progress UI to the import wizard so users can paste portfolio URLs and watch extraction happen in real-time.

Purpose: This is how users trigger AI extraction — paste URLs, see progress, then review results (review modal is Plan 03).
Output: StepUrlPaste component + ExtractionProgress component + updated ImportWizard
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-extraction/04-RESEARCH.md
@.planning/phases/04-ai-extraction/04-01-SUMMARY.md
@src/components/import/ImportWizard.tsx
@src/components/import/Step1Upload.tsx
@src/lib/actions/extraction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StepUrlPaste component for single and bulk URL input</name>
  <files>
    src/components/import/StepUrlPaste.tsx
  </files>
  <action>
Create `src/components/import/StepUrlPaste.tsx` as a client component:

1. **Layout:** Two mode tabs at top: "Single URL" and "Bulk URLs" (default: Single)

2. **Single URL mode (IMPT-09):**
   - One text input for a portfolio URL (placeholder: "https://johndoe.com")
   - Role selector dropdown (same pattern as Step2Mapping — receives `roles` prop)
   - "Extract" button — calls `startSingleExtraction(url, roleId)` server action
   - Basic URL validation: must start with http:// or https://

3. **Bulk URL mode (IMPT-08):**
   - Textarea for pasting multiple URLs, one per line (placeholder: "Paste portfolio URLs, one per line...")
   - Show count: "X URLs detected" (parse by newlines, filter empty, deduplicate)
   - Max 20 URLs enforced — show warning if over limit: "Maximum 20 URLs per batch. Please remove X URLs."
   - Role selector dropdown
   - "Extract All" button — calls `startExtractions(urls, roleId)` server action

4. **On submit (both modes):**
   - Set loading state on button
   - Call server action
   - On success: call `onBatchStarted(batchId)` callback prop — parent wizard transitions to progress view
   - On error: show error via sonner toast

5. **Props interface:**
   ```typescript
   interface StepUrlPasteProps {
     roles: Array<{ id: string; name: string }>;
     onBatchStarted: (batchId: string, candidateId?: string) => void;
   }
   ```

6. **Styling:** Follow existing import step patterns — use Tailwind classes consistent with Step1Upload and Step2Mapping. Card layout with border, rounded corners, padding.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visually inspect StepUrlPaste renders two modes with proper inputs</manual>
  </verify>
  <done>
    - StepUrlPaste renders Single URL and Bulk URL tabs
    - Single mode: URL input + role selector + Extract button
    - Bulk mode: textarea + URL count + 20-URL cap warning + role selector + Extract All button
    - Calls startExtractions/startSingleExtraction on submit
    - Passes batchId to parent via callback on success
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExtractionProgress component + wire URL flow into ImportWizard</name>
  <files>
    src/components/import/ExtractionProgress.tsx
    src/components/import/ImportWizard.tsx
  </files>
  <action>
1. Create `src/components/import/ExtractionProgress.tsx` as a client component:
   - Props: `{ batchId: string; onComplete: (drafts: ExtractionStatusDraft[]) => void }`
   - Define local type `ExtractionStatusDraft` matching the polling API response shape: `{ id, sourceUrl, status, extractedData, error, overallConfidence }`
   - On mount: start polling `GET /api/extraction-status/${batchId}` every 2 seconds using `useEffect` + `setInterval`
   - Store batchId in sessionStorage as `pendingExtractionBatchId` (resume on page revisit, same pattern as wizard)
   - Display:
     a. Overall progress bar: `done / total` with percentage
     b. Status text: "Extracting info from N portfolios..." (while pending > 0), "Extraction complete" (when done === total)
     c. Per-URL status list: each URL as a row with:
        - URL text (truncated to 40 chars)
        - Status icon: spinner (pending/processing), green check (completed), red X (failed)
        - If failed: show error message in muted text
     d. When all done (pending === 0): call `onComplete(drafts)` with the completed drafts array, clear sessionStorage
   - Clean up interval on unmount
   - On mount: check sessionStorage for `pendingExtractionBatchId` — if found, resume polling

2. Update `src/components/import/ImportWizard.tsx`:
   - Add a "URL" tab alongside existing upload modes (File Upload, Paste Text, URL)
   - The URL tab renders `<StepUrlPaste>` instead of Step1Upload
   - Add wizard state for URL extraction flow:
     - New state fields: `extractionBatchId: string | null`, `extractionCandidateId: string | null`, `extractionMode: 'url' | null`
     - When StepUrlPaste calls `onBatchStarted(batchId, candidateId?)`: set extractionBatchId + transition to progress step
   - When in URL extraction mode, the step sequence is: StepUrlPaste -> ExtractionProgress -> (transition to review in Plan 03)
   - For now, ExtractionProgress `onComplete` logs to console and shows a "Review Results" button that stores drafts in state (Plan 03 will wire the review modal)
   - Preserve all existing wizard flow (File Upload + Paste Text -> Mapping -> Validate -> Summary) untouched
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Import wizard shows URL tab. Clicking it shows StepUrlPaste. After submission, ExtractionProgress shows progress bar.</manual>
  </verify>
  <done>
    - ExtractionProgress polls /api/extraction-status/[batchId] every 2s
    - Shows progress bar, per-URL status with icons, and completion state
    - Resumes polling on page revisit via sessionStorage
    - ImportWizard has URL tab alongside File Upload and Paste Text
    - URL flow: StepUrlPaste -> ExtractionProgress -> "Review Results" placeholder
    - Existing import flows (file + paste) are completely unchanged
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Import page at `/import` shows three tabs: File Upload, Paste Text, URL
- URL tab renders StepUrlPaste with single/bulk modes
- After submitting URLs (requires API keys), ExtractionProgress shows and polls
- Existing file upload + paste text flows still work unchanged
</verification>

<success_criteria>
Users can paste portfolio URLs (single or bulk up to 20) in the import wizard, trigger extraction, and see real-time progress. The UI does not block during extraction. The flow is ready for the review modal (Plan 03) to be wired in.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-extraction/04-02-SUMMARY.md`
</output>
