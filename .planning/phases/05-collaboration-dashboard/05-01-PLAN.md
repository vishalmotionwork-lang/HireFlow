---
phase: 05-collaboration-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/format-relative-time.ts
  - src/components/dashboard/activity-feed.tsx
  - src/components/candidates/comment-thread.tsx
  - src/lib/queries/stats.ts
  - src/lib/queries/candidates.ts
  - src/components/candidates/candidate-filter-bar.tsx
  - src/components/candidates/comment-thread.tsx
autonomous: true
requirements:
  - COLB-03
  - SRCH-06
  - DASH-05
  - COLB-01
  - COLB-02
  - COLB-04
  - REJC-01
  - REJC-02
  - REJC-03
  - REJC-04
  - REJC-05
  - PIPE-06
  - CAND-04

must_haves:
  truths:
    - "Typing @ in comment input shows a team member popover and selecting a member inserts @Name into the comment"
    - "Mentions render as blue highlighted text in posted comments"
    - "Import source dropdown appears in filter bar with options: Manual, Excel, CSV, Paste, URL"
    - "Selecting import source filters correctly narrows the candidate list"
    - "getHiredRejectedByRole query returns per-role hired/rejected counts with tier breakdown"
    - "formatRelativeTime is a single shared utility used by both ActivityFeed and CommentThread"
  artifacts:
    - path: "src/lib/utils/format-relative-time.ts"
      provides: "Shared relative time formatting utility"
      exports: ["formatRelativeTime"]
    - path: "src/lib/queries/stats.ts"
      provides: "getHiredRejectedByRole query for DASH-05"
      exports: ["getDashboardStats", "getRoleCandidateCounts", "getRoleTierBreakdown", "getHiredRejectedByRole"]
    - path: "src/lib/queries/candidates.ts"
      provides: "importSource filter param in getCandidates"
      contains: "importSource"
    - path: "src/components/candidates/candidate-filter-bar.tsx"
      provides: "Import source dropdown in filter bar"
      contains: "SOURCE_OPTIONS"
  key_links:
    - from: "src/components/candidates/comment-thread.tsx"
      to: "src/lib/utils/format-relative-time.ts"
      via: "import"
      pattern: "import.*formatRelativeTime.*format-relative-time"
    - from: "src/components/dashboard/activity-feed.tsx"
      to: "src/lib/utils/format-relative-time.ts"
      via: "import"
      pattern: "import.*formatRelativeTime.*format-relative-time"
    - from: "src/components/candidates/candidate-filter-bar.tsx"
      to: "getCandidates"
      via: "URL search param source"
      pattern: "source"
---

<objective>
Build the data layer and component enhancements needed before the dashboard can be completed: extract shared utility, add @mention support to comments, add import source filter, and create the hired/rejected summary query.

Purpose: This plan builds the query and component foundation that Plan 02 (dashboard completion) depends on. It also addresses all already-built requirements (REJC-01-05, COLB-01/02/04, PIPE-06, CAND-04) by verifying they work correctly — the research confirmed these are already implemented.
Output: Extended queries, @mention-capable CommentThread, import source filter, shared formatRelativeTime utility.
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-collaboration-dashboard/05-RESEARCH.md
@src/lib/queries/stats.ts
@src/lib/queries/candidates.ts
@src/components/candidates/candidate-filter-bar.tsx
@src/components/candidates/comment-thread.tsx
@src/components/dashboard/activity-feed.tsx
@src/lib/constants.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared util + query layer extensions + import source filter</name>
  <files>
    src/lib/utils/format-relative-time.ts
    src/components/dashboard/activity-feed.tsx
    src/components/candidates/comment-thread.tsx
    src/lib/queries/stats.ts
    src/lib/queries/candidates.ts
    src/components/candidates/candidate-filter-bar.tsx
  </files>
  <action>
    1. **Extract formatRelativeTime to shared utility.**
       Create `src/lib/utils/format-relative-time.ts` with the `formatRelativeTime` function (currently duplicated in `activity-feed.tsx` and `comment-thread.tsx`). Export it as a named export. Then update both `activity-feed.tsx` and `comment-thread.tsx` to import from the shared util instead of defining it locally. Remove the local definitions.

    2. **Add getHiredRejectedByRole query to stats.ts.**
       Add a new exported function `getHiredRejectedByRole()` to `src/lib/queries/stats.ts`. It should:
       - Query `candidates` joined with `roles` where `isDeleted = false` and status is `hired` or `rejected`
       - GROUP BY `roleId`, `roles.name`, `status`, `tier`
       - Aggregate results in JS into a per-role shape: `{ roleId, roleName, hired, rejected, juniorHired, seniorHired, hireRate }`
       - For avg days to hire: join `candidateEvents` where `toValue = 'hired'`, compute `event.createdAt - candidate.createdAt` in days. Handle zero hired gracefully (show "—" not NaN).
       - Return type: `RoleHireSummary[]`
       - See research Pattern 5 for the query approach. Use Drizzle's `sql` tag for the avg days sub-query if needed.

    3. **Add importSource filter to getCandidates.**
       In `src/lib/queries/candidates.ts`:
       - Add `importSource?: string[]` to `GetCandidatesParams`
       - In the conditions builder, add: `if (importSource && importSource.length > 0) { conditions.push(inArray(candidates.source, importSource)) }`
       - The candidates table already has a `source` column with values: 'manual' | 'excel' | 'csv' | 'paste' | 'url'

    4. **Add import source dropdown to CandidateFilterBar.**
       In `src/components/candidates/candidate-filter-bar.tsx`:
       - Add a `SOURCE_OPTIONS` constant: `[{ value: "manual", label: "Manual" }, { value: "excel", label: "Excel" }, { value: "csv", label: "CSV" }, { value: "paste", label: "Paste" }, { value: "url", label: "URL" }]`
       - Read `source` from URL search params (comma-separated like status)
       - Add a multi-select dropdown (same pattern as Status dropdown using DropdownMenuCheckboxItem) between the Duplicates toggle and the Search input
       - Include the source filter in the `activeFilterCount` calculation
       - The role page server component must pass `importSource` from searchParams to `getCandidates` — check `src/app/roles/[slug]/page.tsx` and `src/app/master/page.tsx` to wire the param through.
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify formatRelativeTime import works in both files, no duplicate definitions remain</manual>
  </verify>
  <done>
    - formatRelativeTime extracted to shared util, both consumers updated
    - getHiredRejectedByRole query exists and handles zero-data edge case
    - getCandidates accepts importSource filter param
    - CandidateFilterBar has import source multi-select dropdown
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @mention support to CommentThread</name>
  <files>
    src/components/candidates/comment-thread.tsx
    src/lib/constants.ts
  </files>
  <action>
    1. **Add TEAM_MEMBERS constant to constants.ts.**
       Add a `TEAM_MEMBERS` array to `src/lib/constants.ts`:
       ```
       export const TEAM_MEMBERS = [
         { id: "vishal", name: "Vishal" },
         { id: "zeel", name: "Zeel" },
         { id: "zaid", name: "Zaid" },
         { id: "kunal", name: "Kunal" },
       ]
       ```
       This is a mock constant consistent with MOCK_USER pattern. Comment that it should be replaced with Clerk user list when auth is added.

    2. **Add @mention detection to CommentThread input.**
       In `src/components/candidates/comment-thread.tsx`:
       - Import `TEAM_MEMBERS` from constants
       - Add state: `showMentionPopover: boolean`, `mentionFilter: string`, `mentionAnchorIdx: number`
       - On input change: detect if user just typed `@` or is typing after `@`. Use `input.selectionStart` to find the `@` character position relative to cursor. Set `mentionFilter` to the text after `@` up to cursor.
       - Show a simple dropdown/popover below the input when `showMentionPopover` is true, filtered by `mentionFilter` (case-insensitive match on name).
       - On selecting a team member: replace the `@partial` text with `@Name ` (with trailing space), close popover.
       - Close popover on Escape key or when text no longer has an active `@` trigger.
       - Keep it simple — a positioned div below the input, not a full portal. Use absolute positioning.

    3. **Add @mention rendering in CommentItem.**
       Create a `renderCommentBody` function that splits comment body on `(@\w+)` regex pattern. For each part starting with `@`, render as `<span className="text-blue-600 font-medium">{part}</span>`. Use this function in CommentItem's body rendering instead of plain text.

    4. **Extract mentions before sending.**
       Before calling `createComment`, parse the comment body to extract mentions:
       ```
       function extractMentions(body: string): Array<{userId: string, name: string}> {
         return TEAM_MEMBERS.filter(m => body.includes(`@${m.name}`))
           .map(m => ({ userId: m.id, name: m.name }))
       }
       ```
       Pass the extracted mentions to `createComment(candidateId, newComment, extractMentions(newComment))`.
       The `createComment` action already accepts a `mentions` parameter.
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open a candidate drawer, type @ in comment input, verify team member dropdown appears. Select a name, verify it inserts. Post the comment, verify @Name renders in blue.</manual>
  </verify>
  <done>
    - Typing @ in comment input shows team member popover
    - Selecting a member inserts @Name into input
    - Posted comments render @mentions as blue highlighted text
    - Mentions are extracted and passed to createComment server action
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. `formatRelativeTime` is imported from shared util in both activity-feed.tsx and comment-thread.tsx (no local definitions)
3. getCandidates query accepts importSource param
4. CandidateFilterBar renders import source dropdown
5. getHiredRejectedByRole returns valid data shape
6. CommentThread supports @mention input, selection, rendering, and extraction
</verification>

<success_criteria>
- All TypeScript compilation passes
- formatRelativeTime deduplicated to single shared utility
- Import source filter dropdown works in filter bar
- @mention popover appears on @ keystroke in comment input
- @mention text renders with blue highlight in posted comments
- getHiredRejectedByRole query available for Plan 02's dashboard table
</success_criteria>

<output>
After completion, create `.planning/phases/05-collaboration-dashboard/05-01-SUMMARY.md`
</output>
