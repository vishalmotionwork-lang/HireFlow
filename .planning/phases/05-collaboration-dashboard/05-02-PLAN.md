---
phase: 05-collaboration-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/dashboard/dashboard-client.tsx
  - src/components/dashboard/hired-rejected-table.tsx
  - src/components/dashboard/activity-feed.tsx
  - src/components/dashboard/role-card.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "Dashboard is the default landing page after login"
    - "Clicking a stat card navigates to /master?status=<statusKey> filtering the master view"
    - "Role cards show tier breakdown mini-bar and Add/Import/View All quick actions"
    - "Hired vs Rejected summary table shows per-role counts, hire rate %, Junior/Senior breakdown, and avg days to hire"
    - "Activity feed auto-refreshes every 30 seconds without user interaction"
    - "Clicking an activity item opens the candidate drawer on the dashboard page"
  artifacts:
    - path: "src/components/dashboard/dashboard-client.tsx"
      provides: "Client wrapper for dashboard state (drawer + auto-refresh)"
      contains: "use client"
    - path: "src/components/dashboard/hired-rejected-table.tsx"
      provides: "Hired vs Rejected summary table component"
      exports: ["HiredRejectedTable"]
    - path: "src/components/dashboard/role-card.tsx"
      provides: "Enhanced role card with tier mini-bar and quick actions"
      exports: ["RoleCard"]
    - path: "src/components/dashboard/activity-feed.tsx"
      provides: "Clickable activity feed items"
      contains: "onCandidateClick"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/dashboard/dashboard-client.tsx"
      via: "server component passes pre-fetched data to client wrapper"
      pattern: "DashboardClient"
    - from: "src/components/dashboard/dashboard-client.tsx"
      to: "src/components/candidates/candidate-drawer.tsx"
      via: "selectedCandidateId state"
      pattern: "CandidateDrawer"
    - from: "src/components/dashboard/dashboard-client.tsx"
      to: "router.refresh()"
      via: "30s setInterval"
      pattern: "setInterval.*router\\.refresh"
    - from: "src/components/dashboard/hired-rejected-table.tsx"
      to: "src/lib/queries/stats.ts"
      via: "getHiredRejectedByRole data passed as prop"
      pattern: "RoleHireSummary"
---

<objective>
Complete the dashboard page with all missing features: clickable stats bar, enhanced role cards with tier breakdown, hired vs rejected summary table, auto-refreshing clickable activity feed, and candidate drawer integration.

Purpose: This is the final feature plan for Phase 5 — it transforms the dashboard from a static display into an interactive hiring command center. After this, the team can see real-time stats, drill into filtered views, and open candidate profiles directly from the dashboard.
Output: Fully interactive dashboard with all DASH-* requirements met, auto-refreshing activity feed, candidate drawer on dashboard.
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-collaboration-dashboard/05-RESEARCH.md
@.planning/phases/05-collaboration-dashboard/05-01-SUMMARY.md
@src/app/dashboard/page.tsx
@src/components/dashboard/activity-feed.tsx
@src/lib/queries/stats.ts
@src/components/candidates/candidate-drawer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard refactor — DashboardClient wrapper + enhanced role cards + clickable stats + hired/rejected table</name>
  <files>
    src/app/dashboard/page.tsx
    src/components/dashboard/dashboard-client.tsx
    src/components/dashboard/hired-rejected-table.tsx
    src/components/dashboard/role-card.tsx
    src/components/dashboard/activity-feed.tsx
  </files>
  <action>
    1. **Create RoleCard component.**
       Extract role card rendering from `dashboard/page.tsx` into `src/components/dashboard/role-card.tsx`. Enhance it with:
       - Tier breakdown mini-bar: a horizontal stacked bar showing proportions of Untiered/Junior/Senior/Both in muted colors (gray/blue/purple/teal). Use data from `getRoleTierBreakdown()` passed as prop. Show count labels on hover or as small text below the bar.
       - Quick action links: "Add" (links to `/roles/${slug}?addCandidate=true`), "Import" (links to `/import`), "View All" (links to `/roles/${slug}`). Style as small text links below the card content.
       - Keep "Create New Role" card as a separate element in the grid (already exists, just preserve it).

    2. **Make stats bar cards clickable (DASH-02).**
       In the dashboard rendering, wrap each stat card in a Next.js `<Link>` component. Map each stat to a Master View URL:
       - "Total Candidates" -> `/master` (no filter)
       - "Left to Review" -> `/master?status=left_to_review`
       - "Under Review" -> `/master?status=under_review`
       - "Shortlisted" -> `/master?status=shortlisted`
       - "Hired" -> `/master?status=hired`
       - "Rejected" -> `/master?status=rejected`
       Add `cursor-pointer hover:border-blue-200 transition-colors` to the Card wrapper.

    3. **Create HiredRejectedTable component (DASH-05).**
       Create `src/components/dashboard/hired-rejected-table.tsx`:
       - Receives `data: RoleHireSummary[]` as prop (from `getHiredRejectedByRole()` created in Plan 01)
       - Renders an HTML table with columns: Role, Hired, Rejected, Hire Rate %, Junior Hired, Senior Hired, Avg Days to Hire
       - Hire rate = `hired / (hired + rejected) * 100`, formatted to 1 decimal place. Show "—" if both are 0.
       - Avg days to hire: show "—" if no hired candidates. Otherwise show integer days.
       - Style: clean table with `border-collapse`, gray header row, alternating row backgrounds, right-aligned numbers.
       - Empty state: "No hired or rejected candidates yet" centered message.

    4. **Make ActivityFeed items clickable (PIPE-05, DASH-06).**
       Update `src/components/dashboard/activity-feed.tsx`:
       - Add `onCandidateClick?: (candidateId: string) => void` to `ActivityFeedProps`
       - In each activity item div, add `onClick={() => activity.candidateId && onCandidateClick?.(activity.candidateId)}`
       - Add `cursor-pointer hover:bg-gray-50` only when `activity.candidateId` is non-null (guard against null candidateId — some activity types like batch imports don't have one)
       - Add `role="button"` and `tabIndex={0}` for accessibility when clickable

    5. **Create DashboardClient wrapper.**
       Create `src/components/dashboard/dashboard-client.tsx` as a `"use client"` component:
       - Receives all pre-fetched data as props: `activities`, `stats`, `activeRoles`, `roleCounts`, `tierBreakdown`, `hireSummary`
       - Manages state: `selectedCandidateId: string | null` for the candidate drawer
       - Implements 30s auto-refresh: `useEffect` with `setInterval(() => router.refresh(), 30_000)` using `useRouter()` from `next/navigation`
       - Renders: stats bar (with Link wrappers), role cards grid (using RoleCard), hired/rejected table section, activity feed (passing `onCandidateClick`), and CandidateDrawer (using `selectedCandidateId`)
       - Import CandidateDrawer from `@/components/candidates/candidate-drawer`

    6. **Update dashboard/page.tsx (server component).**
       Refactor `src/app/dashboard/page.tsx` to be a thin data-fetching layer:
       - Keep it as an `async` server component (no "use client")
       - Fetch all data in `Promise.all`: `getDashboardStats()`, `getRoleCandidateCounts()`, `getRoleTierBreakdown()`, `getRecentActivities(10)`, `getHiredRejectedByRole()`, active roles query
       - Pass all data as props to `<DashboardClient />`
       - Remove all inline rendering — DashboardClient handles it all

    CRITICAL: dashboard/page.tsx must remain a server component. All client state (drawer, refresh) lives in DashboardClient. Do NOT add "use client" to page.tsx.
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visit /dashboard. Verify: stats cards are clickable links, role cards show tier mini-bar and quick actions, hired/rejected table renders (or shows empty state), activity feed items are clickable, page auto-refreshes (check network tab for 30s interval).</manual>
  </verify>
  <done>
    - Dashboard page.tsx is a server component fetching all data
    - DashboardClient wrapper handles all client state and rendering
    - Stats bar cards link to /master?status=X
    - Role cards show tier breakdown mini-bar and Add/Import/View All quick actions
    - HiredRejectedTable renders per-role summary with hire rate and avg days
    - ActivityFeed items are clickable (opens candidate drawer when candidateId exists)
    - 30s auto-refresh is active via router.refresh()
    - CandidateDrawer renders on dashboard for activity item clicks
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — Phase 5 complete</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Present the full Phase 5 feature set for user verification. All code changes from Plans 01 and 02 should be complete.
    Features to verify: rejection flow gate, @mention comments, import source filter, dashboard stats linking, role card enhancements, hired/rejected table, auto-refresh activity feed, clickable activity items with candidate drawer.
  </action>
  <verify>
    1. Visit http://localhost:3000 — should land on Dashboard
    2. Stats bar: Click any stat card (e.g., "Left to Review") — should navigate to /master?status=left_to_review with filtered results
    3. Role cards: Each card should show a tier breakdown mini-bar and Add/Import/View All links
    4. Hired/Rejected table: Scroll down on dashboard to see the summary table (may show empty state if no hired/rejected candidates)
    5. Activity feed: Click an activity item — should open candidate drawer on the dashboard page
    6. Auto-refresh: Open browser DevTools Network tab, wait 30s — should see a navigation/refresh request
    7. Comments @mention: Open any candidate drawer, type @ in comment input — should show team member popover. Select a name, post the comment — should render in blue
    8. Import source filter: Visit /roles/[any-role], check filter bar — should have "Source" multi-select dropdown
    9. Rejection flow: Change any candidate status to "Rejected" — rejection modal should appear requiring reason before save
  </verify>
  <done>User approves all 9 verification steps or provides specific feedback for fixes</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Dashboard is the default landing page (DASH-01)
3. Stats bar cards are clickable Links to /master?status=X (DASH-02)
4. Role cards have tier breakdown + quick actions (DASH-03)
5. Create New Role card exists at end of grid (DASH-04)
6. Hired vs Rejected summary table renders (DASH-05)
7. Activity feed auto-refreshes every 30s and items are clickable (DASH-06, PIPE-04, PIPE-05)
8. Rejection modal intercepts status change to Rejected (REJC-01-05)
9. Comments support @mention (COLB-03)
10. Import source filter works in filter bar (SRCH-06)
</verification>

<success_criteria>
- All 20 requirement IDs for this phase are addressed
- Dashboard is fully interactive with clickable stats, role cards, summary table, and activity feed
- Auto-refresh works on 30s interval
- Candidate drawer opens from activity feed clicks
- Human verify passes on all 9 verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/05-collaboration-dashboard/05-02-SUMMARY.md`
</output>
