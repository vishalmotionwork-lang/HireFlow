---
phase: 06-responsive-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/candidates/candidate-drawer.tsx
  - src/components/candidates/candidate-filter-bar.tsx
  - src/components/dashboard/dashboard-client.tsx
  - src/components/layout/topbar.tsx
  - src/app/roles/[roleSlug]/page.tsx
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, RESP-04]

must_haves:
  truths:
    - "On mobile (375px), the sidebar opens as a hamburger-triggered overlay sheet and all nav destinations are reachable"
    - "On mobile, tapping a candidate row opens a full-screen profile view (100dvh) instead of a partial bottom sheet"
    - "On mobile, role cards stack to a single column and the stat bar is readable without horizontal overflow"
    - "On mobile, filter controls are hidden behind a toggle button and expand vertically when opened"
  artifacts:
    - path: "src/components/candidates/candidate-drawer.tsx"
      provides: "Full-screen mobile profile via Sheet with !h-[100dvh]"
      contains: "100dvh"
    - path: "src/components/candidates/candidate-filter-bar.tsx"
      provides: "Vertical filter stacking on mobile"
      contains: "flex-col"
    - path: "src/components/dashboard/dashboard-client.tsx"
      provides: "Mobile-audited stat bar and role cards grid"
      contains: "grid-cols-2"
    - path: "src/components/layout/topbar.tsx"
      provides: "SidebarTrigger visible on all viewport sizes"
      contains: "SidebarTrigger"
  key_links:
    - from: "src/components/candidates/candidate-drawer.tsx"
      to: "src/hooks/use-mobile.ts"
      via: "useIsMobile hook for conditional className"
      pattern: "useIsMobile"
    - from: "src/components/candidates/candidate-filter-bar.tsx"
      to: "URL search params"
      via: "filtersOpen state toggle"
      pattern: "filtersOpen"
---

<objective>
Make all screens mobile-responsive: full-screen candidate profile on mobile, vertically stacking filter bar, mobile-safe stat cards, and verified sidebar hamburger navigation.

Purpose: This is the final phase — every screen must work correctly at 375px viewport width for the team to use HireFlow on mobile devices.
Output: Updated components with responsive Tailwind classes. No new libraries or components needed.
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-responsive-polish/06-RESEARCH.md
@src/components/candidates/candidate-drawer.tsx
@src/components/candidates/candidate-filter-bar.tsx
@src/components/dashboard/dashboard-client.tsx
@src/components/layout/topbar.tsx
@src/hooks/use-mobile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-screen mobile candidate profile + filter bar vertical stacking</name>
  <files>
    src/components/candidates/candidate-drawer.tsx
    src/components/candidates/candidate-filter-bar.tsx
  </files>
  <action>
**candidate-drawer.tsx (RESP-02):**
1. Import `cn` from `@/lib/utils` (already used elsewhere in codebase).
2. Change the SheetContent className to use `cn()` with conditional mobile classes:
   - When `isMobile` is true: `"!h-[100dvh] rounded-t-none border-t-0 w-full overflow-y-auto p-0"` — the `!` important modifier is required because the Sheet component's `data-[side=bottom]:h-auto` has higher CSS specificity in Tailwind v4.
   - When `isMobile` is false: `"sm:max-w-[480px] overflow-y-auto p-0"` (current desktop behavior, unchanged).
3. Keep `side={isMobile ? "bottom" : "right"}` as-is.
4. Do NOT modify `src/components/ui/sheet.tsx` — it is a generated shadcn file.

**candidate-filter-bar.tsx (RESP-04):**
1. Import `cn` from `@/lib/utils` if not already imported.
2. Change the main filter container div (currently `className={...filtersOpen ? "flex" : "hidden md:flex"...}`) to use `cn()`:
   - When `filtersOpen`: `"flex flex-col gap-3 md:flex-row md:flex-wrap md:items-center md:gap-2"` — vertical stack on mobile, horizontal wrap on desktop.
   - When not open: `"hidden md:flex md:flex-wrap md:items-center md:gap-2"` (current behavior).
3. Add a "Done" button at the bottom of the expanded mobile filter panel — only visible on mobile when `filtersOpen` is true: `<button onClick={() => setFiltersOpen(false)} className="mt-1 w-full rounded-md bg-gray-100 py-2 text-sm font-medium text-gray-700 md:hidden">Done</button>`.
4. Wrap the tier pills container in `flex-wrap` to prevent horizontal overflow on small screens: change `"flex items-center gap-1"` to `"flex flex-wrap items-center gap-1"`.
5. Add `className="max-w-[calc(100vw-32px)]"` to DropdownMenuContent elements that could overflow the viewport on mobile (Status and Source dropdowns specifically).
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx next build 2>&1 | tail -5</automated>
    <manual>Open localhost:3000 with Chrome DevTools at 375px width. Verify: (1) clicking a candidate opens a full-screen profile covering the entire viewport, (2) tapping "Filters" button expands filters vertically with a "Done" button, (3) tier pills wrap instead of overflowing.</manual>
  </verify>
  <done>Candidate profile renders full-screen (100dvh) on mobile viewports. Filter bar expands vertically on mobile with a close button. No horizontal overflow on tier pills or dropdowns.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard mobile audit + topbar mobile search visibility</name>
  <files>
    src/components/dashboard/dashboard-client.tsx
    src/components/layout/topbar.tsx
  </files>
  <action>
**dashboard-client.tsx (RESP-03):**
1. Audit the stat bar grid: currently `grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-6`. At 375px (2 columns), each card is ~170px wide. The stat label uses `truncate` which is correct. Verify the `text-2xl` number doesn't overflow. If the label "Total Candidates" truncates at 170px, add `whitespace-nowrap` is NOT needed (truncate handles it). This grid is acceptable — keep as-is unless overflow found.
2. Audit the role cards grid: `grid-cols-1 gap-4 md:grid-cols-2` — this already satisfies RESP-03. Keep as-is.
3. Audit the two-column layout (roles + activity): `grid-cols-1 gap-6 lg:grid-cols-3` — stacks on mobile. Keep as-is.
4. Audit the HiredRejectedTable: if it renders an HTML table, wrap it in `<div className="overflow-x-auto">` to prevent horizontal page scroll on mobile. Check if this wrapper already exists; if not, add it.
5. Ensure the activity feed section has no horizontal overflow on mobile.

**topbar.tsx (RESP-01):**
1. Verify `SidebarTrigger` has no `hidden` class that would hide it on mobile — currently it does not, so the hamburger is already visible on all viewports. Keep as-is.
2. The global search is `hidden sm:block` — hidden on mobile. This is correct: the filter bar has its own mobile search. No change needed.
3. Increase the mobile spacer div touch area — the user avatar button (`h-8 w-8`) is already 32px which is below the 44px minimum. Change to `h-9 w-9` (36px) and add `min-h-[44px] min-w-[44px]` to the outer button to ensure touch target compliance without changing visual size. Apply this with a wrapper: add `className="flex items-center justify-center min-h-[44px] min-w-[44px]"` to the DropdownMenuTrigger's render prop button.
  </action>
  <verify>
    <automated>cd /Users/vishal.motion/HireFlow && npx next build 2>&1 | tail -5</automated>
    <manual>Open localhost:3000/dashboard at 375px. Verify: (1) stat cards show 2 per row with no overflow, (2) role cards stack single column, (3) hired/rejected table scrolls horizontally if wider than viewport, (4) hamburger menu is visible and opens sidebar overlay, (5) user avatar is tappable without difficulty.</manual>
  </verify>
  <done>Dashboard renders correctly at 375px — stat grid 2-col, role cards 1-col, tables scroll horizontally. Topbar hamburger visible on mobile. Touch targets meet 44px minimum.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with no TypeScript errors
2. At 375px viewport in Chrome DevTools:
   - Sidebar opens via hamburger icon in topbar
   - Candidate profile fills the entire screen (not partial bottom sheet)
   - Filter bar expands vertically with "Done" button on mobile
   - Dashboard stat cards show 2 per row, role cards stack to 1 column
   - No horizontal page-level scroll on any screen
3. At 1440px (desktop), all existing behavior is unchanged
</verification>

<success_criteria>
- All four RESP requirements (RESP-01 through RESP-04) are addressed
- Build passes with zero errors
- No new npm dependencies added
- No modifications to generated shadcn UI files (sidebar.tsx, sheet.tsx)
- Mobile layout works at 375px, 414px, and 768px breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-polish/06-01-SUMMARY.md`
</output>
