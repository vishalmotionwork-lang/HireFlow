---
phase: 06-responsive-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [RESP-01, RESP-02, RESP-03, RESP-04]

must_haves:
  truths:
    - "Human has verified all four RESP requirements work correctly on a mobile viewport"
    - "No regressions on desktop (1440px) layout"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all responsive changes across mobile and desktop viewports.

Purpose: Phase 6 is the final phase — visual verification by a human ensures the app is genuinely usable on mobile before marking the project complete.
Output: Verification approval or list of issues to fix.
</objective>

<execution_context>
@/Users/vishal.motion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishal.motion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-responsive-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all responsive requirements on mobile and desktop</name>
  <action>Human verifies all RESP-01 through RESP-04 requirements at 375px viewport width and confirms no regressions at 1440px desktop width.</action>
  <what-built>Complete mobile responsive polish for all HireFlow screens — full-screen candidate profile, collapsible filter bar, stacking role cards, and sidebar hamburger navigation.</what-built>
  <how-to-verify>
1. Start the dev server: `cd ~/HireFlow && npm run dev`
2. Open http://localhost:3000 in Chrome
3. Open DevTools (Cmd+Option+I) and toggle device toolbar (Cmd+Shift+M)
4. Set viewport to iPhone SE (375px wide)

**RESP-01 — Sidebar Navigation:**
- Tap the hamburger icon (top-left in topbar)
- Sidebar should open as a left-side overlay sheet
- Verify all nav items are visible: Dashboard, each role, Import, Settings
- Tap outside to close — should dismiss

**RESP-02 — Full-Screen Candidate Profile:**
- Navigate to any role page (e.g., /roles/video-editor)
- Tap a candidate row
- Profile should open as FULL SCREEN (covers entire viewport, not a partial bottom sheet)
- Scroll within the profile — all sections accessible
- Close button should dismiss back to the list

**RESP-03 — Role Cards Stacking:**
- Navigate to /dashboard
- Stat cards should show 2 per row (6 cards = 3 rows)
- Role cards should stack to single column (1 card per row)
- No horizontal overflow on the page

**RESP-04 — Filter Bar Collapse:**
- Navigate to any role page
- Filter bar should be hidden, with a "Filters" button visible
- Tap "Filters" — controls expand vertically
- Tap "Done" — filters collapse back
- Active filter count badge should still show on the button

**Desktop Check (1440px):**
- Switch DevTools to 1440px width
- Verify sidebar is visible as a persistent panel
- Candidate drawer opens on the right side (~480px wide)
- Filter bar shows all controls inline horizontally
- Dashboard shows role cards in 2 columns

Type "approved" if all checks pass, or describe any issues found.
  </how-to-verify>
  <verify>Human visual inspection at 375px and 1440px viewports</verify>
  <done>Human approves responsive behavior on mobile with no desktop regressions</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Human confirms all RESP-01 through RESP-04 requirements work at 375px and no regressions at 1440px.
</verification>

<success_criteria>
- Human approves the responsive behavior on mobile
- No regressions reported on desktop
- Phase 6 can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-polish/06-02-SUMMARY.md`
</output>
